# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source "http://gitlab.closeli.cc/MTR/CLSDK_Cocoapods_iOS.git"

target 'CLEmpty' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CLEmpty
  pod "CloseliSDK"

end
